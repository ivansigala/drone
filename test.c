/*
 * test.c — superseded by freertos_hello.c (May 2026).
 *
 * The arm-HIL host runner now lives in freertos_hello.c so the project
 * only has one main(). This file is intentionally empty; you can safely
 * remove it from the build.
 */
